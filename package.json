{
  "name": "restless",
  "version": "0.1.0",
  "private": true,
  "description": "P2P escrow where locked funds earn yield in Aave — your escrow, never idle.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/web",
    "build": "npm run build --workspace=packages/web",
    "compile": "npm run compile --workspace=packages/contracts",
    "test": "npm run test --workspace=packages/contracts",
    "test:web": "npm run test --workspace=packages/web"
  },
  "author": "Mihir Sahu",
  "license": "MIT"
}
